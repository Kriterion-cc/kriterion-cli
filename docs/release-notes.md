Kriterion CLI native executables for macOS, Linux, and Windows.

The macOS files do not use an Apple Developer ID and are not notarized.
The Windows file does not use an Authenticode certificate.
Check the SHA-256 digest before you run a downloaded file.

See the installation and verification guides in the source repository.
